package clientHidden;

class Submarine extends Ship {

	/*
	 * Creates a submarine with starting coordinates at row board is passed in
	 * order to know what squares the ship is in
	 */
	public Submarine(int row) {
		super();
		this.size(3);
	}

}
